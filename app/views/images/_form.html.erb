<%= simple_form_for(@image) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
  <div class="fields">
  <%= f.file_field :image %><br />
   </div>
<% end %>
